{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qa-agent-farm.local/schemas/requirements_analysis_result.json",
  "title": "RequirementsAnalysisResult",
  "description": "Strict output contract for the requirements_analysis skill. Mirrors analyst_agent/models.py:RequirementsAnalysisResult. Every acceptance criterion must carry a verbatim evidence quote that analyst_agent/grounding.py verifies against the real source text.",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "overall_confidence", "requires_human_review"],
  "properties": {
    "status": {
      "description": "success requires >=1 criterion; both abstain statuses require acceptance_criteria to be empty. validation_failed is reserved for the harness and must not be emitted by the model.",
      "enum": ["success", "insufficient_information", "conflicting_evidence", "validation_failed"]
    },
    "acceptance_criteria": {
      "type": "array",
      "default": [],
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["statement", "evidence_quote", "source_field", "confidence"],
        "properties": {
          "statement": {
            "type": "string",
            "minLength": 1,
            "description": "The testable criterion, as an assertion about system behavior."
          },
          "evidence_quote": {
            "type": "string",
            "minLength": 1,
            "description": "Verbatim span copied from source_field. Paraphrase is rejected by grounding validation. Minimum 12 characters after whitespace normalization."
          },
          "source_field": {
            "type": "string",
            "minLength": 1,
            "description": "Evidence field the quote came from, e.g. 'description', 'comments[2]'."
          },
          "confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          }
        }
      }
    },
    "missing_information": {
      "type": "array",
      "default": [],
      "description": "Required non-empty when status is insufficient_information.",
      "items": { "type": "string" }
    },
    "overall_confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Below 0.75, requires_human_review is forced true in code by enforce_confidence_gate."
    },
    "requires_human_review": { "type": "boolean" },
    "notes": {
      "type": ["string", "null"],
      "description": "Optional brief context; never a substitute for evidence."
    }
  }
}
